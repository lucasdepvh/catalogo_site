<div class="card">
    <div class="card-header">
        <h5 class="card-title mb-0"><%= "Produto ##{@product.id}" %></h5>
    </div>
    <div class="card-body">
        <%= render 'form', product: @product %>
    </div>
</div>